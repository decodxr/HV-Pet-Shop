import Image from "next/image";import { ArrowUpRight,Quote } from "lucide-react";import { whatsappUrl } from "@/lib/whatsapp";import type { Product,Review,Service } from "@/types";import { RatingStars } from "./ui";
export function ServiceCard({service,index}:{service:Service;index:number}){return <article className="group flex min-h-72 flex-col rounded-card border border-deep/10 bg-white p-7 shadow-sm transition hover:-translate-y-1 hover:shadow-soft"><span className="mb-auto font-serif text-5xl text-clay/45">0{index+1}</span><p className="eyebrow mb-3">{service.category}</p><h3 className="font-serif text-3xl">{service.name}</h3><p className="mt-3 text-sm leading-relaxed text-muted">{service.description}</p><a data-analytics-event="service_inquiry" data-analytics-label={service.name} className="link-arrow mt-6 flex min-h-11 items-center gap-2 font-bold text-forest" href={whatsappUrl(`Olá! Vi o serviço ${service.name} no site da HV Pet Shop e gostaria de saber mais.`)}>Consultar serviço <ArrowUpRight className="size-4"/></a></article>}
export function ProductCard({product}:{product:Product}){return <article className="overflow-hidden rounded-card bg-white">{product.images[0]&&<Image src={product.images[0]} alt={`${product.name} disponível para consulta na HV Pet Shop`} width={600} height={600} sizes="(max-width: 640px) 100vw, 33vw"/>}<div className="p-6"><p className="eyebrow">{product.category}</p><h3 className="mt-2 font-serif text-2xl"><a href={`/produtos/${product.slug}`}>{product.name}</a></h3>{product.price&&<p className="mt-3 font-bold">R$ {product.price.toFixed(2).replace(".",",")}</p>}<a data-analytics-event="product_inquiry" data-analytics-label={product.name} className="mt-5 inline-flex min-h-11 items-center font-bold text-forest" href={whatsappUrl(`Olá! Vi o produto ${product.name} no site da HV Pet Shop e gostaria de mais informações.`)}>Consultar produto →</a></div></article>}
export function ReviewCard({review}:{review:Review}){return <figure className="snap-start rounded-card bg-white p-7 sm:p-9"><Quote className="size-9 text-clay"/><blockquote className="mt-6 font-serif text-2xl leading-snug">“{review.text}”</blockquote><figcaption className="mt-8"><RatingStars/><b className="mt-3 block text-sm">{review.author}</b><span className="text-xs text-muted">Avaliação no Google</span></figcaption></figure>}
