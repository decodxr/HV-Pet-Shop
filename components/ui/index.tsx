import { cn } from "@/lib/utils";
import { Star } from "lucide-react";
import type { HTMLAttributes,ReactNode } from "react";
export function Container({className,...p}:HTMLAttributes<HTMLDivElement>){return <div className={cn("mx-auto w-full max-w-[1240px] px-5 sm:px-8",className)} {...p}/>}
export function Section({className,...p}:HTMLAttributes<HTMLElement>){return <section className={cn("py-20 sm:py-28",className)} {...p}/>}
export function SectionHeader({eyebrow,title,copy,className}:{eyebrow?:string;title:string;copy?:string;className?:string}){return <header className={cn("max-w-2xl",className)}>{eyebrow&&<p className="eyebrow mb-4">{eyebrow}</p>}<h2 className="display text-4xl sm:text-6xl">{title}</h2>{copy&&<p className="mt-5 text-lg leading-relaxed text-muted">{copy}</p>}</header>}
export function RatingStars({withText=false}:{withText?:boolean}){return <div className="flex items-center gap-2" aria-label="5 de 5 estrelas"><span className="flex text-clay">{[1,2,3,4,5].map(i=><Star key={i} className="size-4 fill-current"/>)}</span>{withText&&<span className="text-sm font-bold">5,0 no Google · 9 avaliações</span>}</div>}
export function Button({href,children,variant="primary",className=""}:{href:string;children:ReactNode;variant?:"primary"|"light"|"outline";className?:string}){const v={primary:"bg-forest text-white hover:bg-deep",light:"bg-white text-deep hover:bg-sand",outline:"border border-forest/25 text-forest hover:bg-white"}[variant];return <a href={href} className={`inline-flex min-h-12 items-center justify-center gap-2 rounded-full px-6 py-3 text-sm font-extrabold transition focus-visible:ring-2 focus-visible:ring-clay ${v} ${className}`}>{children}</a>}
