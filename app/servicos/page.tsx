import type {Metadata} from "next";import { PageShell } from "@/components/page-shell";import { Container,Section } from "@/components/ui";import { ServiceCard } from "@/components/cards";import { services } from "@/lib/data";export const metadata:Metadata={title:"Serviços",description:"Conheça e consulte os cuidados disponíveis na HV Pet Shop em Campo Mourão."};export default function Page(){return <PageShell eyebrow="Cuidados" title="Cuidado respeitoso, em cada detalhe." copy="A relação oficial de serviços pode ser atualizada pelo painel. Fale conosco para consultar a disponibilidade."><Section><Container><div className="grid gap-5 md:grid-cols-3">{services.map((s,i)=><ServiceCard key={s.id} service={s} index={i}/>)}</div></Container></Section></PageShell>}
