import type {Metadata} from "next";import { PageShell } from "@/components/page-shell";import { Container,Section,Button } from "@/components/ui";import { products } from "@/lib/data";import { ProductCard } from "@/components/cards";import { whatsappUrl } from "@/lib/whatsapp";export const metadata:Metadata={title:"Produtos",description:"Catálogo de produtos para pets da HV Pet Shop em Campo Mourão."};export default function Page(){return <PageShell eyebrow="Catálogo" title="Escolhas para o bem-estar do seu pet." copy="Nosso catálogo está sendo preparado. O estoque exibido aqui será sempre administrado pela equipe HV."><Section><Container>{products.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map(p=><ProductCard product={p} key={p.id}/>)}</div>:<div className="rounded-card bg-white p-10 text-center"><h2 className="font-serif text-3xl">Novidades em breve.</h2><p className="mx-auto mt-3 max-w-lg text-muted">Enquanto isso, fale com a gente e consulte os produtos disponíveis na loja.</p><Button href={whatsappUrl()} className="mt-6">Consultar pelo WhatsApp</Button></div>}</Container></Section></PageShell>}
