import { notFound } from "next/navigation";import { products } from "@/lib/data";import { PageShell } from "@/components/page-shell";import { Container,Section,Button } from "@/components/ui";import { whatsappUrl } from "@/lib/whatsapp";export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,p=products.find(x=>x.slug===slug);if(!p)notFound();return <PageShell eyebrow={p.category} title={p.name} copy={p.shortDescription}><Section><Container><p className="max-w-2xl leading-7 text-muted">{p.description}</p><Button href={whatsappUrl(`Olá! Encontrei o produto ${p.name} no site da HV Pet Shop. Gostaria de saber mais.`)} className="mt-8">Tenho interesse</Button></Container></Section></PageShell>}
