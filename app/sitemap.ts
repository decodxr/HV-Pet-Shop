import type {MetadataRoute} from "next";import {products} from "@/lib/data";import {siteUrl} from "@/lib/site";export default function sitemap():MetadataRoute.Sitemap{const pages=["","/servicos","/produtos","/sobre","/contato","/privacidade",...products.filter(product=>product.active).map(product=>`/produtos/${product.slug}`)];return pages.map((path,index)=>({url:`${siteUrl}${path}`,lastModified:new Date(),changeFrequency:index?"monthly":"weekly",priority:index?0.8:1}))}
