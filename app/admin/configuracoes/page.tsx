import {AdminShell} from "@/components/admin-shell";
import {siteSettings} from "@/lib/data";
import {saveSettings} from "./actions";

export default async function Page({searchParams}:{searchParams:Promise<{saved?:string;error?:string}>}){const status=await searchParams;return <AdminShell title="Configurações">{status.saved&&<p role="status" className="mb-5 rounded-xl bg-emerald-50 p-4 text-sm font-bold text-forest">Configurações salvas.</p>}{status.error&&<p role="alert" className="mb-5 rounded-xl bg-red-50 p-4 text-sm font-bold text-red-800">Não foi possível salvar. Verifique os dados e tente novamente.</p>}<form action={saveSettings} className="grid gap-6 lg:grid-cols-2"><section className="rounded-card bg-white p-7"><h2 className="font-serif text-2xl">Atendimento e equipe</h2><p className="mt-2 text-sm text-muted">Publique apenas uma fotografia oficial autorizada.</p><label className="mt-6 block text-sm font-bold">Promessa de resposta<textarea name="response_promise" required defaultValue={siteSettings.responsePromise} rows={3} className="mt-2 w-full rounded-xl border border-deep/15 p-3 font-normal"/></label><label className="mt-5 block text-sm font-bold">Foto oficial da Helen/equipe<input name="team_photo" type="file" accept="image/jpeg,image/png,image/webp" className="mt-2 block min-h-12 w-full rounded-xl border border-deep/15 p-3 font-normal"/><span className="mt-2 block text-xs font-normal text-muted">JPEG, PNG ou WebP, até 5 MB. Sem arquivo real, a foto permanece oculta.</span></label></section><section className="rounded-card bg-white p-7"><h2 className="font-serif text-2xl">SEO e compartilhamento</h2><label className="mt-6 block text-sm font-bold">Imagem Open Graph padrão<input name="og_image" type="file" accept="image/jpeg,image/png,image/webp" className="mt-2 block min-h-12 w-full rounded-xl border border-deep/15 p-3 font-normal"/><span className="mt-2 block text-xs font-normal text-muted">Recomendado: 1200×630.</span></label><label className="mt-5 block text-sm font-bold">Título SEO padrão<input name="seo_title" className="mt-2 min-h-12 w-full rounded-xl border border-deep/15 px-3 font-normal"/></label><label className="mt-5 block text-sm font-bold">Descrição SEO padrão<textarea name="seo_description" rows={3} className="mt-2 w-full rounded-xl border border-deep/15 p-3 font-normal"/></label><button type="submit" className="mt-6 min-h-12 rounded-full bg-forest px-6 font-bold text-white">Salvar configurações</button></section></form></AdminShell>}
