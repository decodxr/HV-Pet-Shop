import {login} from "./actions";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="grid min-h-screen place-items-center bg-sand p-5"><form action={login} className="w-full max-w-md rounded-card bg-white p-8 shadow-soft"><p className="eyebrow">Área restrita</p><h1 className="mt-3 font-serif text-4xl">Painel HV</h1>{p.error&&<p className="mt-4 text-sm text-red-700">E-mail ou senha inválidos.</p>}<label className="mt-7 block text-sm font-bold">E-mail<input name="email" type="email" required className="mt-2 min-h-12 w-full rounded-xl border px-4"/></label><label className="mt-4 block text-sm font-bold">Senha<input name="password" type="password" required className="mt-2 min-h-12 w-full rounded-xl border px-4"/></label><button className="mt-6 min-h-12 w-full rounded-full bg-forest font-bold text-white">Entrar</button></form></main>}
